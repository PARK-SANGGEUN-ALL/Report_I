# v54 업데이트 — 첫화면·전체 디자인 가독성 개편 + 기능 중복 정리

## 1. 첫 화면(입력) 전면 개편
기존에는 "① 다크 네이비 풀스크린 히어로(파티클·글로우 애니메이션, 1.65초짜리 순차 등장
연출) → ② 파일 업로드 → ③ 자동 스크롤 점프 → ④ 별도 배경의 입력 폼"으로 이어지는 2단
분리 구조였습니다. 이게 "첫화면부터 편하지 않다"는 느낌의 원인으로 보고 전면 재구성했습니다.

- **단일 화면, 끊김 없는 흐름**: 어두운 풀스크린 연출을 없애고, 밝은 배경에 로고 → 한 줄
  소개 → **번호가 매겨진 3단계 카드**(① PDF 업로드 ② 기본정보 ③ 지원 희망 대학)가 자연스럽게
  이어지는 구조로 바꿨습니다. 업로드 후 화면이 갑자기 점프하지 않습니다.
- 화려한 애니메이션(입자 배경, 글로우 펄스, 1.65초 순차 reveal, 스크롤 유도 문구)을 제거해
  페이지가 뜨자마자 바로 조작 가능하게 만들었습니다.
- Playwright로 데스크톱(1280px)·모바일(390px) 양쪽에서 실제 렌더링을 스크린샷 확인했습니다.
- 모바일 전용 스타일도 새 구조에 맞게 재작성(카드 패딩, 상단 배지 줄바꿈 등).

## 2. 기능 중복 정리
- **죽은 코드 제거**: 사이드바 메뉴에는 없지만 렌더링 코드만 남아있던 «S7 합격 패턴 분석»,
  «S8 지원 대학 맞춤 진단» 섹션을 찾아서 제거했습니다. 두 섹션 모두 어떤 AI phase에서도
  데이터를 채워준 적이 없는(`patternCheck`, `univAnalysis` 필드 자체가 어디서도 생성되지
  않음) 완전한 죽은 코드였습니다.
- **S14 vs S15 구분 명확화**: "희망 학과 분석"과 "대학 유불리 비교" 둘 다 대학 추천 카드를
  보여줘서 중복처럼 느껴질 수 있었습니다. 완전히 통합하기엔 계산 방식이 달라(S14는 AI가
  생성한 순위, S15는 결정론적 로직) 리스크가 있어, 대신 부제목으로 역할을 명확히 구분했습니다:
  S14 = "✏️ 내가 입력한 대학·학과 위주", S15 = "🗺️ 전국 대학군 전체 대상".

## 3. 리포트 화면 가독성 (본문 글자 크기 상향)
분석 리포트 전반에 10~13px의 작은 글자가 광범위하게 쓰이고 있었습니다(코드 전수 조사 결과
10px 15회, 11px 97회, 12px 124회, 13px 96회). 배지·태그처럼 작아도 되는 UI 요소는 그대로
두고, **긴 분석글을 담는 본문 문단**(줄간격 1.6~2.1의 pre-wrap 텍스트 블록)만 정확히 골라
1px씩 상향했습니다: 12px→13px, 13px→14px (총 86곳). 배지·라벨류는 건드리지 않아 레이아웃
깨짐 위험 없이 "읽는 글자"만 커졌습니다.

## 확인한 사항
- 본문 문단 텍스트는 이미 `--ink2`(어두운 회색, 충분한 대비)를 쓰고 있어 대비 문제는 없었습니다.
- `npm run build` 클린 빌드 확인 완료. Playwright로 실제 화면 스크린샷까지 확인했습니다.

## 다음에 더 다듬을 수 있는 부분 (이번엔 시간상 보류)
- 리포트 화면(S1~S15) 자체의 레이아웃 구조(카드 배치·여백)까지 손대는 «전면» 리디자인은
  실제 분석 데이터로 화면을 띄워보며 다듬어야 안전한데, 이번 세션에서는 API 키가 연결된
  실제 분석 결과로 화면을 열어보지 못해 코드 리뷰 기반으로만 진행했습니다. 실제 리포트를
  받아보신 후 "이 화면이 특히 불편하다"고 콕 집어주시면 그 화면부터 우선 손보겠습니다.
