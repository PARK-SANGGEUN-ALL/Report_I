# v61 업데이트 — PDF/HTML 생기부 판독 품질 대폭 강화

"잘 읽혀 오는 게 제일 중요하다"는 말씀에, 실제로 얼마나 원문이 잘리고 있었는지부터 코드를
직접 뜯어봤습니다. 그 결과 생각보다 심각한 병목을 두 곳에서 발견했습니다.

## 🔥 발견한 진짜 문제: 원문이 조용히 잘리고 있었습니다
- 학생부 원문(rawText)을 AI 프롬프트에 넣을 때 **80,000자로 자르고 있었습니다.** 세특을
  성실하게 길게 써준 학생, 즉 이 앱이 가장 잘 분석해줘야 할 "생기부를 열심히 채운 학생"일수록
  3년치 세특+창체+행발 전체 분량이 80,000자를 넘기 쉬워서, 오히려 «가장 열심히 쓴 학생의
  뒷부분(주로 3학년 최근 학기)이 잘려나가는» 역설적인 상황이었습니다.
- 이 한도를 **180,000자로 대폭 상향**했습니다(Claude/Gemini 모두 이 정도 분량은 충분히
  처리할 수 있는 컨텍스트 용량을 가지고 있습니다). 그래도 원문이 이보다 길면(매우 드문
  경우) 이제는 **조용히 자르지 않고 업로드 직후 화면에 "원문이 매우 길어 일부만 반영될 수
  있다"는 안내**를 띄우도록 했습니다 — 최소한 사용자가 알 수 있게.
- 서버 쪽 전체 요청 크기 상한도 200,000자 → 350,000자로 함께 올려서, 커진 원문 한도가
  다른 곳에서 다시 막히지 않도록 했습니다.

## Phase 1(성적·이수 추출)에 더 넉넉한 출력 여유 부여
- 지난 v51에서 속도 개선을 위해 모든 단계의 AI 응답 토큰 한도를 32,000으로 줄였었는데,
  Phase 1은 성적표·이수과목을 «원문 그대로 옮겨 적는» 가장 "판독"에 가까운 단계라, 활동이
  많은 학생은 이 한도 안에서 JSON이 중간에 잘릴 위험이 있었습니다. **Phase 1만 48,000
  토큰으로 되돌리고**, 나머지 해석·분석 단계는 속도를 위해 32,000을 유지했습니다.

## Phase 1 프롬프트에 자체 검증 지시 추가
- "원문에 실제로 몇 개 학기·몇 개 과목의 성적표가 있는지 직접 세어보고, 추출한 개수와
  «일치»하는지 마지막에 다시 확인하라"는 지시를 명시적으로 추가했습니다. 페이지 경계나
  표 마지막 줄(보통 3학년 최근 학기)에서 누락되는 경우가 잦다는 점도 함께 짚었습니다.
- "원점수·평균·표준편차·등급 등 숫자는 원문에 있는 그대로 옮기고, 어림짐작하거나 원문에
  없는 숫자를 만들어내지 말라"는 지시도 추가했습니다.

## 참고 — 지난 세션에서 이미 갖춰져 있던 안전장치들
- AI의 자체 추출과 로컬 파서(pdf.js 기반) 추출을 과목 단위로 병합해, 한쪽이 놓친 과목을
  다른 쪽이 채워 넣는 이중 안전장치(v55)가 이미 있습니다.
- 로컬 파서는 2015 개정(9등급제)·2022 개정(5등급제) 각각의 여러 표기 변형(표준편차 유무,
  성취도 분포 3구간/5구간, 학기열 위치 등)을 정규식으로 다중 지원하고 있습니다.
- HTML(.htm/.html) 업로드 지원(v60)으로, PDF 텍스트 추출이 불안정한 경우 나이스에서 HTML로
  재발급받아 올리면 더 안정적으로 읽힙니다.

## 참고
- `npm run build` 클린 빌드 확인 완료.
- 이번엔 pdf.js의 좌표 기반 행/열 재구성 알고리즘 자체는 건드리지 않았습니다 — 실제 문제
  파일 없이 이 부분을 손대면 오히려 지금 잘 되는 경우를 깨뜨릴 위험이 있어서, 검증 가능한
  변경(한도 상향, 토큰 여유, 자체 검증 지시)만 반영했습니다. 혹시 특정 PDF에서 성적이 이상하게
  읽히는 경우가 있으면 (개인정보 가린) 캡처를 보여주시면 그 케이스에 맞춰 파서를 보강하겠습니다.
