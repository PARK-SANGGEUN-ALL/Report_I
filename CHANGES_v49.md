# v48 업데이트 내역 (2026-08 기준)

업로드해주신 `모두의 선생님의 학생부종합전형 대학별 통합상담자료` (경기·서울·인천교육청 +
울산진학지원단 통합, 1,498p)를 분석해 아래와 같이 반영했습니다.

## 1. 채점(역량 점수) 보수화 — `src/criteria.js`
- `SCORING_RUBRIC` 신설: 학업/탐구/진로/공동체/성장 5개 역량에 **전국 지원자 분포 기준 앵커**를 부여.
  - 예: 학업역량 36~40점(S)은 "전국 상위 3~5%" 수준으로 제한, 대다수 학생은 22~29점(B) 구간에
    위치하도록 명시. 근거(evidenceList)가 부실하면 고득점 금지.
- Phase 3 응답에 `scoreSummary` 필드 추가 — AI가 스스로 "이 점수가 상위 몇 %인지" 자체 점검하도록
  강제. 리포트 화면(S6 역량 탭)에도 노출됩니다.

## 2. 합격 가능성(%) 신설
- `src/prediction2028.js`
  - `estimateProbability(diff)`: 안정/적정/소신/도전/상향 5단계(+"매우 상향" 1단계 추가, 총 6단계)에
    대해 **보수적인 확률 범위**를 반환 (예: 안정 65~80%, 적정 45~60%, 소신 28~42% …).
  - 유불리 판정 임계값(diff 기준선)을 기존보다 한 단계씩 더 엄격하게 조정
    (예: "안정" 판정 기준 diff ≤ -0.8 → **diff ≤ -1.0**).
  - `rankRecommendedUnivs()`: 안정권만 나열하지 않고 **적정→소신→안정→도전** 순으로 정렬한
    "현실적인 대학 추천 순위" 자동 생성 (근거 자동 서술 포함).
  - `DEPT_VARIANCE_WARNING`: 업로드 자료에서 확인된 "같은 대학도 학과별로 최대 1~2등급까지
    합격선이 벌어진다"는 실증 사례를 안내 문구로 추가.
- `src/criteria.js` Phase 5 프롬프트
  - `topUnivCompare.univs[].admissionProbability`, `tierAnalysis.tiers[].admissionProbability` 필드 추가.
  - **`univRecommendRank`**(대학 추천 순위 8개, 순위·확률·근거) / **`deptRecommendRank`**(학과 추천
    순위 5개, 적합도+합격가능성 종합) 신설.
  - 모든 확률은 아래 3번 REAL 데이터를 근거로 산정하도록 지시, "안정권도 80% 넘기지 말 것" 명시.

## 3. 실제 입결 데이터 보강 (업로드 PDF 기반) — `UNIV_EVAL_INSIGHTS`
아래와 같은 **학과별 실제 편차 사례**를 채점·판정 근거로 삽입했습니다 (일부 발췌):
- 중앙대 학종 «융합형»: 국제물류학과 3.08 vs 의학부 1.19 vs 심리학과 1.92 등 학과 간 1등급+ 편차
- 중앙대 학종 «탐구형»: 화학공학과 4.99, 생명과학과 4.09 등 비인기 기초학문은 평균 합격선이
  크게 낮게(후하게) 형성됨을 실증 → "탐구형처럼 예외적으로 후한 전형도 있지만 소수 사례"로
  과잉 해석하지 않도록 채점 원칙에 명시
- 서울과기대: 학종(면접형)이 교과전형 대비 0.5~1.5등급 낮게(후하게) 형성되는 사례
- 국민대 / 홍익대 / 지방 글로컬대전환 국립대 / 과기특성화대 등 데이터도 반영

## 4. 속도 개선
- Phase 2~5는 기존에도 `Promise.all`로 병렬 호출 중이었음(구조는 이미 합리적).
- `api/analyze.js`: `maxTokens` 60,000 → **32,000**으로 축소.
- 일부 과다한 분량 요구치(리포트 편지 4,000자+→3,000자+, 요약편지 800자+→600자+ 등)를
  현실적인 수준으로 낮춰 응답 생성 시간을 단축.

## 5. UI
- S6(역량 탭): `scoreSummary`(채점 근거 자체점검) 노출.
- S15(대학 유불리): 대학 칩마다 "합격가능성 약 N~M%" 표시, "대학 추천 순위" 카드 신설
  (`rankRecommendedUnivs` 기반, 학과 편차 경고 문구 동반).
- 희망학과 분석 탭: "AI 대학 추천 순위" / "AI 학과 추천 순위" 카드 신설(Phase 5의
  `univRecommendRank` / `deptRecommendRank` 렌더링).

## v49 추가 업데이트 — 실제 입결 원자료(대학어디가) 반영
새로 올려주신 `생기부앱.zip` 안의 **`대학어디가(2023~2026)수시입결자료`**(엑셀, 57,952행)와
**`2026 의치한약수 어디가 수시입결`**(의치한약수 전용) 원자료를 분석해 반영했습니다.

- **`src/realIpgyeol.js` 신설**: 135개 대학의 학종(종합) 전형 중 "모집인원이 가장 많은
  대표(플래그십) 전형"을 대학별로 자동 선정 → 2025~2026학년도 등급70(없으면 등급50)을
  **계열(인문/자연)별로 집계**해 중위값·최유리~최불리 범위·표본수를 저장. 의예/치의예/한의예/약학/수의예는
  50개 대학 별도 집계.
  - 니치 전형(농어촌·기회균형·계약학과·특기자 등)은 제외하고 "일반적인 학종 지원자"가 실제로
    마주치는 전형만 반영해, 편향 없는 대표값이 되도록 했습니다.
  - 예: 성균관대 실제 학종(탐구형) 중위 컷은 인문 3.38 / 자연 3.17등급으로, 기존 앱의 추정치(1.6)보다
    훨씬 엄격합니다 — 사용자분이 지적하신 "채점이 너무 후하다"는 문제의 실제 원인이었습니다.
- **`src/prediction2028.js`**: `compareAllUnivs()`가 이제 대학별로 **실제 데이터가 있으면 그것을
  최우선으로 사용**(`dataSource:"real"`)하고, 없는 대학만 기존 추정치로 폴백합니다. 계열 분류
  (`classifyGyeol`)를 새로 추가해 인문/자연 계열을 구분해 비교합니다.
- **UI**: 대학 유불리 칩에 실측 데이터 사용 여부(초록 "실측" 배지)와 학과별 실제 범위를 표시합니다.
- **`UNIV_EVAL_INSIGHTS`(criteria.js)**: 위 실측 데이터베이스에서 뽑은 21개 주요 대학 + 10개
  의치한약수 대학의 실제 중위 컷·범위를 AI 프롬프트에 그대로 주입해, AI가 감이 아니라 실측치를
  1차 근거로 사용하도록 강제했습니다.

### 이번에 반영하지 못한 자료 (용량이 매우 커서 별도 요청 시 추가 작업 필요)
- `2027전공가이드북_모음.zip`(212MB, 11개 대학 공식 전공안내서 PDF: 한양대·인하대·서울시립대·
  홍익대 미술계열·동국대·순천대·세명대·국립경국대·연세대·우송대) — 학과 소개·이수권장과목 등
  세부 내용은 아직 반영 전입니다.
- `2027 수시 대학별 요약-대교협.zip`(36MB, 대교협 공식 202개 대학 수시요강 요약 PDF) — 전형방법·
  모집인원 등 최신 공식 요강이나 이번엔 처리하지 못했습니다.
- `(경기도교육청)_2027수시NAVI_교사용.xlsx`(28MB) — 자체 계산기·데이터 시트가 많아 추가 분석이 필요합니다.
- `홍익대_캠퍼스자율전공_컨퍼런스_공유용.html` — 미반영.

특정 대학이나 학과를 콕 집어주시면 해당 자료를 우선적으로 더 깊게 반영해드릴 수 있습니다.

## 참고
- `npm install && npm run build` 로 정상 빌드 확인 완료 (기존 코드에 있던 인라인 style의
  `border` 중복 키 warning은 원래 코드에 있던 것으로 동작에는 영향 없습니다).
- 여전히 대학·학과별 «최신 3개년 모집요강·입결»은 반드시 각 대학 입학처에서 재확인하시길 권장합니다.
  본 리포트의 확률·순위는 참고용 보수적 추정치입니다.
