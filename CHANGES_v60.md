# v60 업데이트 — 고교유형 선택 버그 수정, HTML 업로드 지원, 이미지 PDF 감지, 전공 범용성 강화

## 1. 고교유형 선택 — 있었는데 망가져 있던 버그를 발견해 수정
확인해보니 고교유형 선택 UI·로직 자체는 이미 만들어져 있었는데, `inSchoolType` 상태가
**같은 파일에 중복 선언**되어 있어 앱이 아예 로드되지 않는 치명적 버그가 있었습니다
(JS에서 같은 이름을 두 번 선언하면 문법 오류가 납니다). 이번에 발견해서 고쳤습니다.

이제 첫 화면 Step 2에서 일반고/자사고/과학고/외고·국제고/영재학교/특성화고/검정고시 중
선택할 수 있고, 이 정보는 AI 분석 시 다음과 같이 반영됩니다:
- **특목고·자사고 선택 시**: "이 학생 학교는 재학생 전체의 학업 수준이 높아 등급이 상대적으로
  낮게(불리하게) 나오는 경향이 있다"는 점을 감안해, 등급 숫자만으로 학업역량을 낮게 평가하지
  않도록 지시. 다른 대학의 실제 합격 등급 데이터를 인용할 때도 "이 학생에게는 그 컷을 그대로
  적용해도 무방하다(오히려 유리할 수 있음)"는 방향으로 해석하도록 반영.
- **일반고 선택 시 (또는 미입력 시)**: 반대로, 인용하는 합격 등급 데이터에 특목고·자사고
  학생이 섞여 있어 실제보다 낮아 보일 수 있다는 점을 감안해 다소 보수적으로 해석하도록 반영.

## 2. HTML(.htm/.html) 파일 업로드 지원 (신규)
나이스 대국민서비스에서 받은 파일이 PDF가 아니라 HTML일 수 있다는 말씀을 반영해 새로
구현했습니다.
- `parser.js`에 `extractHtmlText()` / `parseAllFromHtml()`을 신설 — HTML의 `<table>` 구조를
  그대로 순회하며 행(row)을 열(column) 단위로 재구성합니다. 기존 PDF 파서가 좌표 기반으로
  재구성하던 것과 동일한 형식(탭 구분 열)으로 출력되도록 만들어서, 이미 검증된 성적표·활동
  추출 정규식 로직을 그대로 재사용합니다(중복 구현 없이 안정성 확보).
- 실제 나이스 형식과 유사한 샘플 HTML로 직접 동작 검증(jsdom으로 테스트 후 제거)했습니다 —
  표 헤더, 성적 행이 정상적으로 추출되는 것을 확인했습니다.
- 업로드 화면의 파일 선택기가 `.pdf`와 `.html`/`.htm` 둘 다 받도록 변경했고, 안내 문구도
  "정부24·나이스 대국민서비스 파일을 그대로 업로드하세요"로 수정했습니다.

## 3. 이미지로 스캔된 PDF 감지 (신규)
"학교생활기록부2와 다른 PDF 같고 이미지 같은 것도 있다"는 말씀 — 스캔된 이미지 PDF는 텍스트
레이어가 없어서 기존엔 조용히 실패(빈 결과)했습니다. 이제 텍스트 추출 결과가 거의 없으면
"스캔된 이미지 PDF일 가능성이 높습니다 — HTML(.htm)로 다시 발급받아 업로드해주세요"라고
명확하게 안내합니다. ⚠️ 참고: 이미지 OCR 자체는 이번에 구현하지 않았습니다(정확도·속도
트레이드오프가 커서) — 대신 텍스트가 있는 형식(PDF 텍스트본 또는 HTML)으로 재발급을
안내하는 방식을 택했습니다.

## 4. "모든 학과가 다 있어야" — 특정 사례가 없는 전공도 동일하게 정밀 분석하도록 명시
지금까지 REAL_CASE_STUDIES_2026에 넣은 실제 사례는 전체 전공 중 일부일 뿐입니다(모든 전공의
실제 사례를 다 구할 수는 없습니다). 그래서 이번엔 "사례가 없는 전공을 만나도 절대 대충
분석하지 말라"는 명시적 지시를 추가했습니다 — 사례가 있는 전공은 그 사례를, 없는 전공은
①사례들에서 뽑아낸 계열-공통 구조적 원칙(5단계 사고 프레임, 감상형 vs 연구절차형 구분 등)
②대학 공식 5대 역량 평가 프레임(모든 전공에 공통 적용됨) ③해당 계열 특성에 맞는 응용
(자연과학=실험·증명 중심, 예체능=실기+이론 결합, 교육=현장이해+지도역량 등)으로 똑같이
정밀하게 분석하도록 했습니다.

## 5. 고3 vs 고1·2 생기부 형식 차이 — 추출 유연성 강화
Phase 1의 grades 추출 지시에 "나이스 PDF·HTML은 발급 경로·학년(교육과정)에 따라 열 순서·
표기 방식이 다를 수 있으니 고정된 하나의 형식만 기대하지 말고, 원문에 실제로 어떤 열이 어떤
순서로 나열되어 있는지 스스로 파악해서 유연하게 매칭하라"는 지시를 추가했습니다. (참고:
로컬 파서 자체는 이미 2015/2022 개정 각각의 여러 형식 변형을 정규식으로 다중 지원하고
있었고, AI 추출과 로컬 파서 추출을 과목 단위로 병합하는 구조(v55에서 추가)가 있어 한쪽이
특정 형식을 못 읽어도 다른 쪽이 보완하는 이중 안전장치가 이미 갖춰져 있습니다.)

## 참고
- `npm run build` 클린 빌드 확인 완료.
- OCR(이미지 텍스트 인식) 자체 지원은 이번 범위에 포함하지 않았습니다 — 필요하시면 별도로
  검토해드리겠습니다(클라이언트 사이드 OCR은 정확도가 들쭉날쭉해서, 서버사이드 OCR API
  연동이 더 안정적일 수 있습니다).
