# v53 업데이트 — 가독성 개선 (긴 문단 → 짧은 줄글)

## 질문 1: 분석 속도, 실제로 개선됐나?
네, v51에서 두 가지를 바꿨습니다 (아직 안 보셨다면 다시 확인 부탁드려요):
- Gemini 호출을 3단계 캐스케이드(Flash→Pro→Flash-Lite, 최악의 경우 3번 재시도)에서 1단계로 축소.
- 호출 순서를 Gemini 우선 → **Claude 우선**으로 변경(Claude가 1차, 실패 시에만 Gemini 1회 폴백).
  즉 예전엔 최악의 경우 phase당 최대 4번(Gemini 3단계 + Claude) 순차 호출까지 갔지만, 지금은
  최대 2번(Claude → Gemini)입니다.
- `maxTokens` 60,000 → 32,000, 일부 과도한 분량 요구치도 축소(v48).
- Phase 2~5는 원래부터 `Promise.all`로 병렬 호출 중이라 구조 자체는 이미 합리적이었습니다.

## 질문 2: 분석글이 긴 문단형태라 가독성이 떨어짐
이건 실제로 프롬프트 설계상 허점이 있었습니다. 그동안 "500자+ 분량" 같은 요구치는 있었지만,
AI가 그 500자를 «한 덩어리 문단»으로 몰아쓰는 걸 막는 장치가 약했습니다. 이번에 손봤습니다:

- **문단 분할 기준을 강화**: "2~3문장마다 줄바꿈" → **"1~2문장(60~100자)마다 반드시 줄바꿈"**으로
  전체 프롬프트에 걸쳐 일괄 수정. "한 문단 100자 초과 금지"를 명시적으로 못박았습니다.
- **분량 요구치의 의미를 재정의**: "500자+" = "한 덩어리로 500자"가 아니라 "짧은 줄글 여러 개를
  합쳐 500자"라는 점을 프롬프트에 명시했습니다.
- **가장 긴 필드(reportLetter 3000자+, letter 1000자+, summaryLetter 600자+)**: 기존엔 ①~⑫
  같은 큰 섹션 단위로만 줄바꿈을 요구해서, 섹션 «내부»가 오히려 450~500자짜리 통짜 문단이 될
  위험이 있었습니다. 이제 섹션 내부에서도 1~2문장마다 추가로 줄바꿈하도록 명시해, 12개 섹션이
  실제로는 30~40개의 짧은 단락으로 읽히게 했습니다.
- **공통 가독성 원칙 신설**(`h.common`을 통해 모든 5단계 분석에 공통 적용): 문장을 접속사·쉼표로
  억지로 길게 이어붙이지 말 것, 나열식 정보는 "①②③"이나 "·" 같은 짧은 목록형 표기를 섞어 쓸 것,
  "빽빽한 벽 같은 텍스트"가 아니라 "짧은 문단이 이어지는 글"을 최우선 목표로 삼을 것.

⚠️ 다음 분석부터 반영됩니다 — 이미 저장된 과거 분석 결과에는 소급 적용되지 않으니, 가독성
개선을 확인하시려면 새로 분석을 돌려봐 주세요.
