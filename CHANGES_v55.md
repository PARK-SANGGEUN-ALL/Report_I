# v55 업데이트 — 표시 버그 정정, 채점 정확도, 최종 추천표, 실제 후기 웹검색

캡처해주신 화면을 보고 발견한 실제 버그들과, 이어서 요청하신 기능들을 모두 반영했습니다.

## 1. 표시 버그 (캡처에서 직접 확인된 것들)
- **`**볼드**`가 그대로 텍스트로 보이던 문제**: 원인을 찾았습니다 — 리포트 화면 33곳에서
  마크다운 강조 파서(`hl()`)를 거치지 않고 AI 텍스트를 그대로 렌더링하고 있었습니다. 전수
  조사해서 33곳 모두 `hl()`을 적용하도록 수정했습니다.
- **"일강원대학교사범대학부설고등학교" 같은 이상한 학교명**: 지난 개인정보 보호 작업에서
  생긴 부작용이었습니다 — 학교명을 AI 프롬프트에서 완전히 가리다 보니 AI가 참고할 실제 정보가
  없어 그럴듯한 이름을 지어내고 있었습니다. 이제 화면에 표시되는 학교명은 AI가 뭐라고 답하든
  **항상 브라우저에서 직접 추출한 실제 값**을 우선 사용하도록 고쳤습니다.
- **"김민우은" 같은 조사 오류**: 실명 치환 로직이 "학생은/이/을/과/으로"를 실명으로 단순
  치환만 하고 받침 유무에 따른 조사(은/는, 이/가, 을/를, 과/와, 으로/로 — ㄹ받침 예외 포함)를
  맞추지 않아서 생긴 문제였습니다. 받침 유무를 판별해 조사를 자동 교정하는 함수를 만들어
  적용했고, 여러 이름 패턴(모음 끝, 자음 끝, ㄹ받침)으로 직접 수치 검증했습니다.

## 2. 대학 비교 카드의 헷갈리는 두 숫자
큰 숫자(적합도 %)와 작은 숫자(합격가능성 %)가 뒤섞여 있어 "합격 가능성이 높다"는 착각을
줄 수 있었습니다. **합격가능성을 큰 숫자로, 적합도는 "서류 매칭점수(참고용, 합격률 아님)"로
작게 명시**하도록 시각적 우선순위를 뒤집었습니다. AI 프롬프트에도 두 수치가 방향성이
어긋나지 않도록(적합도는 높은데 합격가능성이 극단적으로 낮으면 재검토) 지시를 추가했습니다.

## 3. 역량 점수가 여전히 높게 나오던 진짜 원인
화면 상단에 크게 뜨는 "83점 A등급" 같은 총점이, 실은 엄격한 채점 기준(SCORING_RUBRIC)이 적용된
5개 역량 점수의 합이 아니라 **Phase 1이 별도로, 기준 없이 생성하던 독립적인 숫자**였습니다.
이제 표시되는 총점은 반드시 Phase 3의 실제 5개 역량 점수 합계로 재계산됩니다.

## 4. 역량 채점 — 플러스/마이너스 근거 명시 (요청하신 형태)
각 역량에 "기본점수 → 가점 요인(구체적 근거+점수) → 감점 요인(구체적 근거+점수) → 최종 점수"
형태의 산정 근거(`scoreBreakdown`)를 추가했고, base + 가점 - 감점 = 최종 점수가 실제로
일치해야 한다는 산수 검증 규칙도 프롬프트에 넣었습니다. 화면에도 이 내역이 표로 보입니다.
"감점 요인 없음"으로 제출하려면 정말 없는지 재검토하라는 지시도 추가해 무조건 감점 없이
후하게 주는 것을 막았습니다.

## 5. 종합리포트 — 최종 결론 표 + 피할 대학
편지 형식은 그대로 유지하면서, **표 최상단에 "대학·학과·전형·지원라인·합격가능성·근거"
10개짜리 최종 추천표**와 "현재로선 피하는 게 좋은 대학·학과" 3~5개를 새로 추가했습니다.
"어떤 대학의 어떤 학과를 어떤 전형으로 지원하는 게 이상적인가"라는 최종 질문에 리포트가
직접 답하도록 만들었습니다.

## 6. 기능 중복 정리 — "이수 현황" → "이수 기반 추천학과"
말씀하신 대로 tab을 바꿨습니다. 그런데 확인해보니 별도로 있던 "학과 적합도"(S9) 탭이
정확히 같은 데이터(recommendedMajors)를 다른 디자인으로 또 보여주고 있었습니다. 더 상세한
쪽(S9의 성적분석·강점/보완점·액션플랜 포함 버전)을 살려서 "이수 기반 추천학과" 탭으로
통합하고, 중복이던 S9 탭은 삭제했습니다.

## 7. 누락된 교과 성적 (3학년 1학기 확률과 통계 등)
원인: 기존 로직은 AI가 반환한 grades 배열이 **완전히 비어있을 때만** 로컬 파서 결과로
대체했는데, 일부 과목만 빠진 경우(예: 3-1 확률과 통계)는 이 안전장치가 작동하지 않았습니다.
이제 AI 결과와 로컬 파서 결과를 **과목 단위로 합쳐서** 누락된 과목을 채워 넣습니다. Phase 1
프롬프트에도 "3학년 1학기(최근 학기)는 특히 누락되기 쉬우니 원문의 과목 수와 추출 개수가
일치하는지 다시 세어보라"는 지시를 추가했습니다.

## 8. 실제 합격/불합격 후기 웹 검색 (신규 기능)
네이버 검색 API(블로그+카페)를 연동해, 입력하신 지원 희망 대학·학과에 대해 "학생부종합
합격 후기" / "불합격 스펙" 류의 실제 게시물을 검색하고 그 내용을 Phase 5 분석의 보조 근거로
활용하도록 만들었습니다. **다만 이 기능을 실제로 켜려면 아래 설정이 필요합니다:**

1. [네이버 개발자센터](https://developers.naver.com/apps/#/register)에서 애플리케이션 등록
   (사용 API: "검색") → `Client ID`, `Client Secret` 발급 (무료)
2. Vercel 프로젝트 → Settings → Environment Variables에 `NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET`
   추가 후 Redeploy

설정하지 않으면 이 기능은 조용히 건너뛰고 나머지 분석은 그대로 동작합니다(필수 아님).
익명 후기 특성상 AI가 이를 "절대적 사실"이 아니라 "~라는 후기가 있음" 식으로 조심스럽게
인용하도록 지시해뒀습니다.

## 참고
- `npm run build` 클린 빌드 확인 완료.
- README.md의 환경변수 안내도 최신 상태(Claude 1순위, NAVER 키 등)로 갱신했습니다.
