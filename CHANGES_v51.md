# v51 업데이트 — 보안/개인정보 강화 + 5등급제 실측 변환 + 정성평가 자료 반영

## 1. 개인정보 보호 강화 (요청: "학생 생기부 저장·로그·재사용·제3자 노출 최소화")
- **`api/analyze.js` 전면 재작성**
  - 서버 로그에서 학생부 원문·AI 응답 "내용"을 완전히 제거. 이제 길이·상태코드 등 메타데이터만 기록됨
    (과거엔 파싱 실패 시 AI 응답의 첫 800자/끝 400자를 그대로 로그에 남겨, 세특 인용문이 그대로
    Vercel 로그에 노출될 수 있었음 — 가장 심각했던 leak을 제거).
  - CORS `Access-Control-Allow-Origin: '*'` → `ALLOWED_ORIGIN` 환경변수 화이트리스트로 변경.
  - 응답에 `Cache-Control: no-store` 등 무캐시 헤더 추가 — 중간 프록시/CDN에 결과가 남지 않도록 함.
  - 선택적 `APP_SHARED_SECRET` 헤더 검증 — 설정 시 제3자가 API를 직접 호출해 데이터를 흘리거나
    비용을 소모시키는 것을 차단.
  - Gemini 호출을 3단계 캐스케이드(Flash→Pro→Flash-Lite)에서 1단계로 축소하고, Anthropic(Claude)을
    1순위로 변경 — 동일 학생부가 여러 외부 AI 엔드포인트로 반복 전송되는 것을 최소화.
    (Anthropic API는 기본적으로 고객 데이터를 학습에 쓰지 않는다고 명시하고 있어 더 안전한 기본값으로 판단)
  - `pdfB64`(원본 PDF) 관련 서버 코드 제거 — 애초에 클라이언트도 원본 PDF를 서버로 전송하지 않음(확인 완료).
  - 과도하게 큰 요청은 즉시 거부(비정상 트래픽 방지).
- **`src/criteria.js` — 실명·학교명 최소화**
  - AI로 보내는 프롬프트에서 학생 실명·학교명을 항상 고정 플레이스홀더("학생"/"OO고등학교")로
    치환(`redactPII`). 학생부 원문(rawText) 안의 실명·학교명도 best-effort로 치환.
  - `restorePII`로 AI 응답을 받은 뒤 **로컬(브라우저)에서만** 실제 이름/학교로 복원해 화면에 표시
    (`App.jsx`에 재귀 치환 로직 적용). 서버·AI로는 실명이 다시 전송되지 않음.
  - ⚠️ 정직한 한계 고지: 학생부 원문 자체를 LLM에 보내 분석하는 구조이므로 "AI 제공사가 아예 아무것도
    못 보게" 만들 수는 없습니다. 위 조치들은 ①로그에 남는 정보 ②전송 횟수/대상 ③제3자 노출 경로를
    최소화한 것이며, 완전한 제로 트러스트를 원하신다면 자체 호스팅 LLM 구성이 필요합니다.

## 2. 5등급제 ↔ 9등급제 실측 변환표 (부산광역시교육청 자료 반영)
- 기존에는 손으로 추정한 변환표를 사용했으나, **부산 88개교 14,331명 실측 데이터**(現 고1
  1~2학기 5등급제 누적 vs 同 기수 9등급제 등급평균)로 교체(`GRADE5_TO_9_ANCHORS`).
- `convert5To9()` / `convert9To5()` 양방향 함수 제공. 예: 5등급제 2.00등급 ≈ 9등급제 3.35등급
  (상위 17.4%), 5등급제 1.00등급 유지 학생은 3학년까지 누적 시 전체의 0.3~0.6% 수준.
- **버그 수정**: 구현 중 보간 함수가 축 파라미터를 무시하고 항상 배열의 0/2번 위치만 읽는 버그를
  발견해 수정함(9→5 역변환이 완전히 틀린 값을 반환하고 있었음). 배포 전 수치 검증으로 확인·수정.
- `GRADE_SYSTEM_NOTE` 신설: 現 고3(9등급제, 현재 대입)과 現 고1·고2(5등급제, 2028학년도 대입)를
  같은 숫자로 비교하지 말 것, 입결 언급 시 반드시 환산값과 원 체계를 함께 표기할 것을 AI에 강제.

## 3. 정성평가 실제 자료 반영 (서울대 간담회·동국대 연구·사고력 프레임·모의평가 시스템)
`UNIV_EVAL_INSIGHTS`(Phase 5)와 신설된 `QUALITATIVE_EVAL_SIGNALS`(Phase 2·3·4 공통 삽입)에 반영:
- **서울대 2026.05 입학사정관 간담회**: 성취수준-서술 정합성("아무리 봐도 B 수준인데 100점이면
  B로 평가"), 2인칭/3인칭 관찰자 시점 구분(부모·학원 개입 신호), 보고서 개수보다 질·확장,
  표준편차 인위적 조정의 역효과, 진로 편중 세특의 역설(글자 수만 낭비), 선발이 아닌 육성 관점 등.
- **동국대 수행평가 영역명 범주화 연구**: 실증 분석으로 도출된 「내용/주제」(매체·정보·의견·언어·
  문학·독서·진로·의사소통·성찰) × 「방법」(글쓰기·논술비평·말하기·포트폴리오·과정평가·토의토론·제작)
  키워드 매트릭스 — 세특·수행평가명을 더 명확하게 짓는 실질적 가이드로 반영.
- **사고력 40키워드 5단계 프레임**(문제정의→탐구설계→분석→확장→성찰): Phase 2(활동분석)·
  Phase 3(역량채점)·Phase 4(탐구주제 설계)에서 학생 활동이 어느 단계까지 도달했는지 진단하고
  부족한 단계(특히 확장·성찰)를 채우도록 조언하는 데 반영.
- **학종 모의 서류평가 시스템 실사용 시사점**(경북여고, 2026.07): 유사교육과정 지원자 비교,
  교과성적-세특 정합성 대조, 동일 문장(붕어빵 세특) 탐지 기능, 출결의 간접적 성실성 신호 등.

## 이번에도 반영하지 못한 자료 (용량이 커서 요청 시 추가 작업 필요)
- `정성평가_학습자료.zip` 내 고려·중앙·숭실대 사정관 간담회(hwpx), 2027 이화여대 학생부위주전형
  안내서 — 아직 미반영.
- `2028업데이트.zip`(113MB, 2028 대입전형시행계획 원문 등) — 아직 미반영.
- 이전 업데이트에서 안내드린 11개 대학 전공가이드북, 대교협 202개 대학 요약, 수시NAVI 등도 계속 보류 중.

특정 대학·자료를 콕 집어주시면 그 부분을 우선적으로 반영해드리겠습니다.
